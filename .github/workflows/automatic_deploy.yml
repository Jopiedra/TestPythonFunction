name: Automatic Deploy
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Deploy
#         run: ./github-actions-script.sh
        shell: cmd
        run: ./deploy.bat
