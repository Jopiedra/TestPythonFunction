name: Automatic Deploy
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      
#       - name: Granted access
#         run: git update-index --chmod=+x ./github-actions-script.sh
        
      - name: Run Shell file 
        shell: cmd
        run: ./deploy.bat
#         run: ./github-actions-script.sh

#         run: |
#           echo 1. What is in this directory?
#           ls
